# ═══════════════════════════════════════════════════════════════
# 🛡️ WARRIOR ENS API - ENVIRONMENT CONFIGURATION
# ═══════════════════════════════════════════════════════════════

# Server Configuration
NODE_ENV=development
PORT=3000
WS_PORT=8080

# RPC Providers (comma-separated for fallback)
RPC_PROVIDER_1=https://eth.llamarpc.com
RPC_PROVIDER_2=https://rpc.ankr.com/eth
RPC_PROVIDER_3=https://cloudflare-eth.com
RPC_PROVIDER_4=https://ethereum.publicnode.com

# Redis Configuration (optional - falls back to in-memory)
USE_REDIS=false
REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=your_redis_password
# REDIS_TLS=true

# Cache Settings
CACHE_TTL=300
CACHE_CHECK_PERIOD=60

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
ALLOWED_ORIGINS=*
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# WebSocket Settings
WS_HEARTBEAT_INTERVAL=30000
WS_MAX_CLIENTS=1000

# Logging
LOG_LEVEL=info
# LOG_FORMAT=json

# Security
HELMET_ENABLED=true
# API_KEY=your_secret_api_key

# Monitoring (optional)
# SENTRY_DSN=https://your-sentry-dsn
# GRAFANA_URL=https://your-grafana-instance

# Custom Settings
# MAX_BATCH_SIZE=10
# REQUEST_TIMEOUT=30000
